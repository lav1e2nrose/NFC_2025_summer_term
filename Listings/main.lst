C51 COMPILER V9.60.7.0   MAIN                                                              09/07/2025 15:25:27 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE MAIN
OBJECT MODULE PLACED IN .\Objects\main.obj
COMPILER INVOKED BY: E:\Keil\C51\BIN\C51.EXE main.c OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTEXTEND PRINT(.\Listings\main.ls
                    -t) TABS(2) OBJECT(.\Objects\main.obj)

line level    source

   1          #include <reg52.h>
   2          #include "uart.h"
   3          #include "bluetooth.h"
   4          #include "AT24C02.h"
   5          #include "oled.h"
   6          #include "ui.h"
   7          #include "utils.h"
   8          
   9          // 定义按键
  10          sbit KEY1 = P3^0;  // 发送按键
  11          sbit KEY2 = P3^1;  // 接收按键
  12          
  13          // 全局变量
  14          CARD receivedCard;   // 接收到的卡片数据
  15          
  16          /**
  17           * @brief 检测按键
  18           * @return 按键值：1表示KEY1，2表示KEY2，0表示无按键
  19           */
  20          u8 KEY_Scan(void)
  21          {
  22   1          if (KEY1 == 0)
  23   1          {
  24   2              delay_ms(10);  // 消抖
  25   2              if (KEY1 == 0)
  26   2              {
  27   3                  while (KEY1 == 0);  // 等待按键释放
  28   3                  return 1;
  29   3              }
  30   2          }
  31   1          
  32   1          if (KEY2 == 0)
  33   1          {
  34   2              delay_ms(10);  // 消抖
  35   2              if (KEY2 == 0)
  36   2              {
  37   3                  while (KEY2 == 0);  // 等待按键释放
  38   3                  return 2;
  39   3              }
  40   2          }
  41   1          
  42   1          return 0;  // 无按键按下
  43   1      }
  44          
  45          /**
  46           * @brief 主函数
  47           */
  48          void main(void)
  49          {
  50   1          u8 key;
  51   1          
  52   1          // 初始化各个模块
  53   1          UI_Init();          // 初始化用户界面
  54   1          Bluetooth_Init();   // 初始化蓝牙
C51 COMPILER V9.60.7.0   MAIN                                                              09/07/2025 15:25:27 PAGE 2   

  55   1          
  56   1          // 进入主循环
  57   1          while (1)
  58   1          {
  59   2              // 检测按键
  60   2              key = KEY_Scan();
  61   2              
  62   2              if (key == 1)  // 按下KEY1，发送卡片数据
  63   2              {
  64   3                  UI_Update(UI_SENDING);
  65   3                  Bluetooth_SendAllCards();  // 发送所有卡片数据
  66   3                  UI_Update(UI_IDLE);
  67   3              }
  68   2              
  69   2              // 处理蓝牙接收
  70   2              Bluetooth_Process();
  71   2              
  72   2              // 根据蓝牙状态更新UI
  73   2              if (Bluetooth_GetStatus() == BT_SENDING)
  74   2              {
  75   3                  UI_Update(UI_SENDING);
  76   3              }
  77   2              else if (Bluetooth_GetStatus() == BT_FINISHED)
  78   2              {
  79   3                  // 接收完成，显示接收到的数据
  80   3                  UI_ShowReceived(receivedCard);
  81   3              }
  82   2              else if (Bluetooth_GetStatus() == BT_IDLE)
  83   2              {
  84   3                  UI_Update(UI_IDLE);
  85   3              }
  86   2          }
  87   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    124    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =     20    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
